import { View, Text } from "react-native";
import React from "react";
import { CText } from "@/src/shared/ui/components/CText";

const TotalTab = () => {
  return (
    <View style={{}}>
      <CText shade={50} style={{ textAlign: "center" }}>
        TotalTab
      </CText>
    </View>
  );
};

export default TotalTab;
